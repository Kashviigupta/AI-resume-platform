import React from 'react'

// Placeholder page — full implementation arrives in its dedicated milestone
// (UploadResume -> Milestone 6, History -> Milestone 11, Settings -> later polish).
export default function Settings() {
  return (
    <div className="card p-6 text-sm text-ink/60 dark:text-paper/60">
      Settings page — coming in a later milestone.
    </div>
  )
}
